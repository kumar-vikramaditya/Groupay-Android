<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="10dip" 
        android:id="@+id/linear_eventmembers">

        <Button
            android:id="@+id/btn_upcoming_events"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="5dip"
            android:background="#ffffff"
            android:gravity="start"
            android:text="Upcoming Events" />

        <ListView
            android:id="@+id/list_upcoming_events"
            android:layout_width="wrap_content"
            android:layout_height="100dp" >
        </ListView>

        <Button
            android:id="@+id/btn_past_events"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dip"
            android:background="#ffffff"
            android:gravity="start"
            android:text="Past Events" />

        <ListView
            android:id="@+id/list_past_events"
            android:layout_width="wrap_content"
            android:layout_height="100dp" >
        </ListView>
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/linear_eventmembers"
        android:layout_alignParentBottom="true"
        android:orientation="horizontal"
        android:paddingTop="10dip" >

        <Button
            android:id="@+id/btnCreateEvent"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="5dip"
            android:text="Create Event" />
    </LinearLayout>

</RelativeLayout>